FROM alpine:3.4
RUN apk update && apk upgrade && apk add build-base ruby ruby-dev ruby-irb
RUN apk update && apk upgrade && apk add nginx
ADD bespoked.rb /usr/bin/bespoked.rb
RUN chmod 500 /usr/bin/bespoked.rb
CMD ["/usr/bin/bespoked.rb"]
